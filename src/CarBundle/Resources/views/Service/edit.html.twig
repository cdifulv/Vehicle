{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Model\Service edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('Service_index', {'vehicleId': entity.vehicle.id}) }}">
            Back to Service List
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
